name: GitHub Actions Demo1
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  Test:
    runs-on: self-hosted
    steps:
      - run: apt update -y
      - run: ifconfig
